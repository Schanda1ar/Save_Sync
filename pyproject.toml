[project]
name = "save-sync"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14,<3.15"
dependencies = [
    "PySide6>=6.8.0",
    "psutil>=7.0.0",
    "pydrive2>=1.21.3",
]

[dependency-groups]
dev = [
    "pytest>=8.3.5",
]
